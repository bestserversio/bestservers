import { type ServerPublic } from "~/types/Server";

export default function ServerView({
    server
} : {
    server: ServerPublic
}) {
    return (
        <div>
            
        </div>
    );
}